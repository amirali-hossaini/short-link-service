# توضیحات سیستم کوتاه‌کننده لینک

## 1. این سیستم از نظر شما چه بخش‌هایی لازم دارد؟

همانطور که اشاره شد، در این سیستم یک لینک به عنوان ورودی دریافت می‌شود و در خروجی، نسخه کوتاه‌شده آن به کاربر تحویل داده خواهد شد. برای پیاده‌سازی چنین سیستمی، وجود چند بخش اصلی ضروری است.

در ابتدا نیاز به یک سرویس یا API وجود دارد که وظیفه دریافت لینک، اعتبارسنجی، تولید لینک کوتاه و مدیریت درخواست‌ها را بر عهده داشته باشد. همچنین یک دیتابیس برای ذخیره‌سازی اطلاعات لینک‌ها موردنیاز است تا بتوان ارتباط بین لینک اصلی و لینک کوتاه را نگهداری کرد.

علاوه بر این، سیستم باید بخشی برای مدیریت ریدایرکت داشته باشد تا هنگام باز شدن لینک کوتاه، کاربر به آدرس اصلی هدایت شود. همچنین در صورت افزایش تعداد کاربران و لینک‌ها، استفاده از سیستم‌هایی مانند کش، لاگ‌گیری و مانیتورینگ نیز می‌تواند برای بهبود عملکرد و پایداری سیستم مفید باشد.

## 2. چه اطلاعاتی باید ذخیره شود؟

در یک سیستم کوتاه‌کننده لینک، مهم‌ترین بخش، ذخیره‌سازی اطلاعات مربوط به لینک‌ها و مدیریت درخواست‌های کاربران است. حداقل اطلاعاتی که باید در دیتابیس ذخیره شود شامل موارد زیر می‌باشد:

-   لینک اصلی (Original URL) که کاربر وارد می‌کند
-   لینک کوتاه شده (Short URL)
-   تاریخ ایجاد لینک
-   تعداد بازدیدها یا کلیک‌ها
-   وضعیت لینک (فعال یا غیرفعال)
-   شناسه کاربر ایجادکننده لینک در صورت وجود سیستم احراز هویت

همچنین بسته به نیاز سیستم، می‌توان اطلاعات بیشتری نیز ذخیره کرد، از جمله:

-   تاریخ انقضا برای لینک
-   آی‌پی یا اطلاعات آماری بازدیدکنندگان
-   تعداد کلیک‌های یکتا
-   دستگاه یا مرورگر کاربر
-   پارامترهای UTM و Query String
-   دامنه سفارشی برای لینک کوتاه

ذخیره این اطلاعات باعث می‌شود علاوه بر انجام عملیات ریدایرکت، امکاناتی مانند آمارگیری، مدیریت لینک‌ها، محدودسازی، تحلیل رفتار کاربران و گزارش‌گیری نیز در سیستم قابل پیاده‌سازی باشد.

## 3. وقتی کاربر لینک کوتاه را باز می‌کند، سیستم باید چه مراحلی را انجام دهد؟

مراحل کلی به این صورت خواهد بود:

1.  بررسی وجود لینک در دیتابیس  
2.  بررسی وضعیت لینک  
3.  ثبت اطلاعات آماری و تحلیلی  
    در این مرحله اطلاعات مورد نیاز سیستم ذخیره می‌شود، از جمله:
    -   آی‌پی کاربر
    -   زمان بازدید
    -   مرورگر کاربر
    -   سیستم‌عامل و نوع دستگاه
    - و غیره ...
4.  افزایش تعداد بازدید لینک  
5.  ریدایرکت کاربر به لینک اصلی  

> در صورتی که لینک وجود نداشته باشد یا غیرفعال باشد، سیستم باید کاربر را به صفحه خطا مانند 404 یا صفحه مخصوص لینک منقضی‌شده هدایت کند.


## 4. اگر دو نفر یک لینک یکسان ثبت کنند، به نظر شما خروجی باید یکی باشد یا متفاوت؟ چرا؟

این موضوع به نحوه طراحی سیستم و نیازمندی‌های پروژه بستگی دارد. در حالت کلی دو رویکرد مختلف وجود دارد:

### حالت اول: خروجی یکسان باشد

در این حالت، اگر دو کاربر یک لینک کاملاً یکسان ثبت کنند، سیستم همان لینک کوتاه قبلی را بازمی‌گرداند.  
این روش باعث می‌شود:

-   از ایجاد داده‌های تکراری جلوگیری شود
-   حجم دیتابیس کمتر شود
-   تعداد لینک‌های کوتاه غیرضروری کاهش پیدا کند

اما در این روش آمارگیری و تحلیل داده‌ها به‌صورت دقیق امکان‌پذیر نخواهد بود، زیرا مشخص نیست هر بازدید مربوط به کدام کاربر یا کدام ثبت‌کننده لینک بوده است. به همین دلیل این رویکرد بیشتر برای سیستم‌های ساده و عمومی مناسب است که وابستگی خاصی به کاربران ندارند.

### حالت دوم: خروجی متفاوت باشد

در صورتی که سیستم دارای کاربران و حساب کاربری باشد، بهتر است هر کاربر لینک کوتاه مخصوص به خود را داشته باشد؛ حتی اگر لینک اصلی یکسان باشد.  

در این حالت، هر لینک به شناسه کاربر متصل می‌شود و آمار، تعداد بازدیدها و مدیریت لینک‌ها برای هر کاربر به صورت جداگانه قابل انجام خواهد بود.

برای مثال:

-   دو کاربر مختلف می‌توانند برای یک URL مشابه، دو لینک کوتاه متفاوت داشته باشند
-   آمار بازدید هر کاربر مستقل ثبت می‌شود
-   هر کاربر امکان مدیریت لینک‌های خودش را خواهد داشت

همچنین اگر یک کاربر لینکی را قبلاً ثبت کرده باشد و دوباره همان لینک را وارد کند، سیستم می‌تواند همان لینک کوتاه قبلی را به او بازگرداند تا از ایجاد رکوردهای تکراری جلوگیری شود.


## 5. چه خطاهایی ممکن است رخ دهد و چطور باید با آن‌ها برخورد کرد؟

برخی خطاهای رایج در سیستم کوتاه‌کننده لینک عبارت‌اند از:

-   نامعتبر بودن لینک ورودی → اعتبارسنجی URL
-   تکراری بودن کد کوتاه → تولید مجدد کد
-   پیدا نشدن لینک → نمایش خطای 404
-   غیرفعال یا منقضی بودن لینک → جلوگیری از ریدایرکت
-   خطاهای دیتابیس → مدیریت Exception و ثبت لاگ
-   حملات اسپم → Rate Limiting و CAPTCHA
-   افزایش زیاد درخواست‌ها → استفاده از Cache و Queue

هدف اصلی این است که سیستم پایدار بماند و خطاها به‌درستی مدیریت شوند.


## 6. اگر تعداد لینک‌ها زیاد شود، چه مشکلاتی ممکن است پیش بیاید؟

با افزایش تعداد لینک‌ها و بازدیدها، ممکن است سیستم با مشکلاتی مانند کاهش سرعت، افزایش فشار روی دیتابیس و افت پرفورمنس مواجه شود.  
این مشکلات را می‌توان با استفاده از Cache، ایندکس‌گذاری مناسب دیتابیس، Queue و بهینه‌سازی کوئری‌ها تا حد زیادی مدیریت و حل کرد.
